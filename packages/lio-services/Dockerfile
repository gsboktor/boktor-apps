# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build lio-services`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t lio-services`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0

WORKDIR /app

RUN addgroup --system lio-services && \
          adduser --system -G lio-services lio-services

COPY dist/packages/lio-services lio-services/
RUN chown -R lio-services:lio-services .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix lio-services --omit=dev -f install

CMD [ "node", "lio-services/main.js" ]
